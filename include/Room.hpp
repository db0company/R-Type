#ifndef _ROOM_HPP__
#define _ROOM_HPP__

#include <map>
#include "GameParameter.hpp"
#include "user/User.hpp"

//todo
class Room
{
public:
  Room(void);
  ~Room(void);
  Room(Room const &);
  Room& operator=(Room const &);
private:
};

#endif // _ROOM_HPP
